import 'package:flutter/material.dart';

class FactumMartSettingsModule extends StatelessWidget {
  const FactumMartSettingsModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Text('Settings'),
    );
  }
}
