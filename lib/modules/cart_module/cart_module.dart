import 'package:flutter/material.dart';

class FactumMartCartModule extends StatelessWidget {
  const FactumMartCartModule({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Text('Cart'),
    );
  }
}