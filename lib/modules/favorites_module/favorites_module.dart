import 'package:flutter/material.dart';

class FactumMartFavoritesModule extends StatelessWidget {
  const FactumMartFavoritesModule({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Text('Favorites'),
    );
  }
}